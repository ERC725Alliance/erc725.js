// put inside here


/// export a simple send function, that returns a promise:


// pass in the key, and outcomes the result


// route from here, to the specifi provider

// for the provider types: check for the providers

// dapp running the library 

// when we pass in the URL, and it's a come up with a schema... pass in an object, with type graph:

/*

{
  type:
  url:
}

for web3

passing in web3.currentProvider

we use the prototype name

does it have request/send
new version is providerObject (type, URL)

*/
